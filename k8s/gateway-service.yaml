apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: llm-inference-poc
  labels:
    app: gateway
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
