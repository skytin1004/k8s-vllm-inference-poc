apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: llm-inference-poc
  labels:
    app: vllm
spec:
  type: ClusterIP
  selector:
    app: vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
